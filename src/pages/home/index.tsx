import * as React from 'react';
import { Page } from '../../components';

export class HomePage extends React.Component {
    render() {
        return (
            <Page name="home">
                home page
            </Page>
        );
    }
}
